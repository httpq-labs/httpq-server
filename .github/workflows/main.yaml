name: main
on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    container: eclipse-temurin:17

    services:
      postgres:
        image: postgres:14.1
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: Run Maven Tests
        env:
          DATABASE_URL: postgresql://postgres:5432/postgres_test?user=postgres&password=postgres
        run: ./mvnw test
